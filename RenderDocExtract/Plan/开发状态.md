# 开发状态

## 当前状态

- 当前阶段：材质属性 HLSL 切片增强完成：公共贴图采样函数、正确 PrimitiveID 提取、StructuredBuffer Load 静态求值、UE Material Function 风格输出均已通过验证。
- 当前脚本：`D:/UGit/renderdoc/RenderDocExtract/Scripts/material_hlsl_slice.py`
- 当前测试 RDC：`D:/PTGameDoc/TLUS2/Pix/TLUS2-PIX/WuKong_Forest0/build/RDC/WuKong_Forest1.rdc`
- 固定测试 EID：`7643`、`7955`
- Instanced Draw 规则：`7955` 为 instanced Draw；第一阶段仅提取 `extracted_instance=0`，同时 manifest 保留原始 `numInstances`。
- 日志规则：所有脚本默认写入 `D:/UGit/renderdoc/RenderDocExtract/Logs/脚本名_YYYYMMDD_HHMMSS.log`；支持 `--no-log` 和 `-Nolog` 关闭。
- Mesh 格式规则：无损权威数据为 `mesh.json + attributes.bin/json`；OBJ 仅预览；GLB/glTF 推荐作为后续交换格式；FBX 只作为可选后处理，不作为默认主格式。
- 是否等待用户确认：否。
- 下一步：用户检查增强版 `material_slices.hlsl` 后，继续推进更多像素/primitive 上下文采样、UE Material Function 导入格式化或批量化/GLB 后处理。

## 脚本进度

| 顺序 | 脚本 | 状态 | 测试状态 | 用户确认 | 备注 |
|---:|---|---|---|---|---|
| 1 | `rd_session.py` | completed | passed | not_required | qrenderdoc 直接运行通过；EID 7643/7955 均找到，7955 numInstances=18，extracted_instance=0 |
| 2 | `library_db.py` | completed | passed | not_required | self-test 通过：重复注册不重复，next index 稳定递增 |
| 3 | `extract_shaders.py` | completed | passed | confirmed | EID 7643 VS/PS raw/disasm/HLSL 输出成功；PS JSON 已含 render_target_count=6 和 RT 格式；用户已确认继续 |
| 4 | `extract_textures.py` | completed | passed | confirmed | EID 7643 提取 4 个 PS texture binding，4 个唯一 DDS；重复运行未新增文件；用户已确认继续 |
| 5 | `extract_buffers.py` | completed | passed | confirmed | EID 7643 提取 16 个 buffer binding、16 个稳定唯一 buffer 资产，7 个 cbuffer decoded JSON；重复运行未新增文件；用户已确认继续 |
| 6 | `extract_mesh.py` | completed | passed | confirmed | 用户清空 meshes 后已重跑；EID 7643/7955 均输出非零 OBJ + mesh.json/attributes sidecar；7955 记录 numInstances=18/extracted_instance=0；用户已确认继续 |
| 7 | `build_draw_manifest.py` | completed | passed | confirmed | EID 7643 draw_manifest 生成成功，warnings=0，引用 shader/texture/buffer/mesh，RT=6，Blend=opaque；用户已确认继续 |
| 8 | `extract_eid.py` | completed | passed | not_required | EID 7955 全链路集成通过，5 步均 returncode=0，warnings=0，manifest 记录 numInstances=18/extracted_instance=0 |
| 9 | `validate_extraction.py` | completed | passed | not_required | EID 7643/7955 均通过文件引用、hash、VS/PS texture/buffer 分阶段绑定与 RenderDoc live PipeState 一致性验证 |
| 10 | `gbuffer_layout_match.py` + `Config/gbuffer_layouts.json` | completed | passed | not_required | 根据新版 UE GBuffer 文档建立 6 种 layout 和 channel/unpack schema；支持 layout_id+RT+channel 查询；EID 7643/7955 PS 均 exact 匹配 `ue5_legacy_velocity_no_precshadow` |
| 11 | `material_hlsl_slice.py` + `validate_material_slices.py` + `resolve_hlsl_resource.py` + `extract_primitive_context.py` | completed | passed | review_requested | 公共 TextureSamples/SampleMaterialTextures 去重采样；正确提取 PrimitiveID(7643=708,7955=3964)；StructuredBuffer Load 静态求值；EID 7643/7955 验证通过 |

## 状态更新规则

每完成一个脚本开发、自测或用户确认，都必须更新本文件：

- `当前状态`
- `脚本进度` 表格
- 若遇到错误，同时追加到 `D:/UGit/renderdoc/RenderDocExtract/Plan/踩坑.md`
- 每个脚本默认生成 log 到 `D:/UGit/renderdoc/RenderDocExtract/Logs/`，除非用户传入 `--no-log` 或 `-Nolog`

关键人工确认门禁：

1. `extract_shaders.py` 自测通过后停止，等待用户确认 shader 文件夹里 HLSL/raw/disasm 输出正确。
2. `extract_textures.py` 自测通过后停止，等待用户确认 DDS 与 texture library 输出正确。
3. `extract_buffers.py` 自测通过后停止，等待用户确认 buffer raw/decoded JSON 输出正确。
4. `extract_mesh.py` 自测通过后停止，等待用户确认 OBJ 与 attributes sidecar 输出正确。
5. `build_draw_manifest.py` 自测通过后停止，等待用户确认 draw_manifest 内容正确。
